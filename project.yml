name: MatchMate

options:
  bundleIdPrefix: com.kumarsaurabh
  deploymentTarget:
    iOS: "17.0"

targets:
  MatchMate:
    type: application
    platform: iOS

    sources:
      - path: MatchMate

    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.kumarsaurabh.MatchMate
        SWIFT_VERSION: 5.0
        TARGETED_DEVICE_FAMILY: "1,2"